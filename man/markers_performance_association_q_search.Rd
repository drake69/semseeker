% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markers_performance_association_q_search.R
\name{markers_performance_association_q_search}
\alias{markers_performance_association_q_search}
\title{calculate scores for the same dataset at changing of quantile/bins}
\usage{
markers_performance_association_q_search(
  inference_details,
  result_folder,
  dest_folder,
  folder_key,
  pvalue_column = "PVALUE_ADJ_ALL_BH",
  aggr_fun = "mean",
  significance = TRUE,
  sql_conditions = c(),
  study,
  qs = c(2, 4, 8, 16, 32),
  ...
)
}
\arguments{
\item{inference_details}{data.frame where each row describes one inference run
(columns: depth_analysis, family_test, transformation_y, covariates, independent_variable)}

\item{result_folder}{character string path to the root result folder}

\item{dest_folder}{character string path to the destination folder for aggregated output}

\item{folder_key}{character suffix appended to the study name to build per-q sub-folder names}

\item{pvalue_column}{name of the adjusted p-value column to use for significance ranking; default "PVALUE_ADJ_ALL_BH"}

\item{aggr_fun}{aggregation function name (as a string) used to summarise metrics across areas; default "mean"}

\item{significance}{logical; if TRUE, restrict comparison to significant results only}

\item{sql_conditions}{character vector of SQL condition strings used to subset samples}

\item{study}{character string identifying the study}

\item{qs}{integer vector of quantile/bin counts to evaluate; default c(2,4,8,16,32)}

\item{...}{additional arguments passed to \code{init_env}}
}
\value{
A data.frame of performance metrics aggregated per marker and
  quantile/bin number; results are also written to CSV files on disk.
}
\description{
calculate scores for the same dataset at changing of quantile/bins
}
