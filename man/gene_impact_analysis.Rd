% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_impact_analysis.R
\name{gene_impact_analysis}
\alias{gene_impact_analysis}
\title{Gene impact and pathway enrichment analysis}
\usage{
gene_impact_analysis(
  inference_details,
  adjust_per_area_s,
  adjust_globally_s,
  pvalue_columns,
  adjustment_methods,
  alphas,
  study,
  significance,
  statistic_parameter,
  path_dbs,
  phenolyzer_folder_bin,
  disease,
  phenolyzer = FALSE,
  WebGestalt = FALSE,
  pathfindr = FALSE,
  STRINGdb = FALSE,
  Phenolyzer_STRINGdb = FALSE,
  Phenolyzer_WebGestalt = FALSE,
  ctdR = FALSE,
  result_folder,
  maxResources = 90,
  parallel_strategy = "multicore",
  ...
)
}
\arguments{
\item{inference_details}{data.frame. Inference parameter table (must contain
a \code{depth_analysis} column; rows with \code{depth_analysis == 3} are
processed).}

\item{adjust_per_area_s}{logical vector. Whether to adjust p-values per area
for each \code{pvalue_columns} entry.}

\item{adjust_globally_s}{logical vector. Whether to adjust p-values globally
for each \code{pvalue_columns} entry.}

\item{pvalue_columns}{character vector. Column name(s) in the inference
results to use as p-value filter.}

\item{adjustment_methods}{character vector. Multiple-testing correction
method(s) applied (e.g. \code{"BH"}).}

\item{alphas}{numeric vector. Significance thresholds to iterate over.}

\item{study}{character. Study identifier used for labelling outputs.}

\item{significance}{logical. Whether to filter by significance.}

\item{statistic_parameter}{character. Statistic column used for ranking.}

\item{path_dbs}{character. Path database(s) for pathfindR.}

\item{phenolyzer_folder_bin}{character. Path to the Phenolyzer binary
directory (required when \code{phenolyzer = TRUE} or related flags).}

\item{disease}{character. Disease keyword passed to Phenolyzer.}

\item{phenolyzer}{logical. Enable Phenolyzer backend (default \code{FALSE}).
Requires Phenolyzer installed system-wide.}

\item{WebGestalt}{logical. Enable WebGestalt backend (default \code{FALSE}).}

\item{pathfindr}{logical. Enable pathfindR backend (default \code{FALSE}).}

\item{STRINGdb}{logical. Enable STRINGdb backend (default \code{FALSE}).}

\item{Phenolyzer_STRINGdb}{logical. Enable combined Phenolyzer+STRINGdb
backend (default \code{FALSE}).}

\item{Phenolyzer_WebGestalt}{logical. Enable combined Phenolyzer+WebGestalt
backend (default \code{FALSE}).}

\item{ctdR}{logical. Enable CTDbase backend via the \pkg{ctdR} package
(default \code{FALSE}).}

\item{result_folder}{character. Path to the SEMseeker result folder.}

\item{maxResources}{numeric. Maximum percentage of CPU cores to use
(default 90).}

\item{parallel_strategy}{character. Parallelisation backend (default
\code{"multicore"}).}

\item{...}{Additional named arguments passed to \code{init_env()}.}
}
\value{
Invisibly \code{NULL}. Pathway enrichment results are written to the
  pathway sub-folder of \code{result_folder}.
}
\description{
Identify genes impacted by stochastic epigenetic mutations and
  run downstream pathway / phenotype enrichment using one or more backends:
  WebGestalt, STRINGdb, pathfindR, CTDbase (ctdR), and Phenolyzer.

  \strong{Phenolyzer requirement:} the \code{phenolyzer} and
  \code{Phenolyzer_STRINGdb} / \code{Phenolyzer_WebGestalt} backends require
  Phenolyzer to be installed and configured at the operating-system level
  (see \url{https://phenolyzer.wglab.org}). Supply the path to the
  Phenolyzer binary directory via \code{phenolyzer_folder_bin}.
}
\examples{
result_dir <- tempdir()
\donttest{
gene_impact_analysis(
  inference_details  = inference_df,
  adjust_per_area_s  = TRUE,
  adjust_globally_s  = FALSE,
  pvalue_columns     = "PVALUE_ADJ_ALL_BH",
  adjustment_methods = "BH",
  alphas             = 0.05,
  result_folder      = "~/semseeker_results/",
  WebGestalt         = TRUE
)
}
}
