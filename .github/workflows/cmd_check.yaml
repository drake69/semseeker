# This workflow is superseded by R-CMD-check.yml.
# Disabled to avoid duplicate runs.
on:
  workflow_dispatch:

name: cmd_check (disabled)

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See R-CMD-check.yml."
