# This workflow is disabled. Use R-CMD-check.yml instead.
# Keep this file for reference only (triggered manually if needed).
on:
  workflow_dispatch:

name: all-actions (disabled)

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See R-CMD-check.yml."
